# Flyway migrations

Add versioned PostgreSQL migrations here when the first business entities are
implemented. Flyway is enabled for normal application startup; Hibernate only
validates the resulting schema.
